services:
  postgres:
    image: postgres:16-alpine
    container_name: juniperfox-postgres
    environment:
      POSTGRES_USER: juniperfox
      POSTGRES_PASSWORD: juniperfox_dev
      POSTGRES_DB: juniperfox
    ports:
      - "5432:5432"
    volumes:
      - juniperfox_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U juniperfox -d juniperfox"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  juniperfox_pgdata:
